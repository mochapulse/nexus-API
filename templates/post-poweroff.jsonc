// REAL RESPONSE EXAMPLE — POST /api/v1/power/poweroff
// DEBUG=true  : this exact payload is returned (stub, nothing is executed).
// Production  : same payload after `systemctl poweroff` succeeds.
// On systemctl failure: HTTP 500 {"status":"error","detail":"<error>"}
{
  "poweroff_triggered": "true"
}
